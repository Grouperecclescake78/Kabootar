# The standard Flutter lint set. See
# https://dart.dev/tools/linter-rules for the full catalogue.
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Trailing commas are enforced by `dart format`, not as an analysis error.
    require_trailing_commas: ignore
